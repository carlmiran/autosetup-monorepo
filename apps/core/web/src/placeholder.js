// AUTOSETUP — apps/core/web (placeholder honesto)
//
// Este app NÃO tem o scaffold real do Next.js 16 ainda — decidido em
// SPR-CORE-001/EBK 0.1 como stack de frontend, mas a implementação real
// (create-next-app, roteamento, shadcn/ui) é tarefa da PRÓXIMA sprint,
// não desta (ambiente base). Isso é uma pendência explícita, não uma
// simulação de app funcionando.
console.log("[autosetup-web] PENDENTE: scaffold real do Next.js 16. Ver docs/traceability.md.");
