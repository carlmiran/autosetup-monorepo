{
  "name": "@autosetup/adapter-llm",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "clean": "rm -rf dist",
    "test": "tsx src/gateway.test.ts"
  },
  "dependencies": {
    "@autosetup/types": "workspace:*",
    "@autosetup/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0"
  }
}
