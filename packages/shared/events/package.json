{
  "name": "@autosetup/events",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@autosetup/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
